{% extends 'admin/base.html.twig' %}

{% block main %}
<h2>Videos / My liked videos</h2>

<div class="table-responsive">
  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th>#</th>
        <th>Video name</th>
        <th>Link</th>
{#        <th>Category</th>#}
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
{% for video in videos %}
      <tr>
        <td><?=$v?></td>
        <td>{{ video.originFilename }}</td>
        <td><a target="_blank" href="{{ path('videos.show', {'id': video.id}) }}">go to video</a></td>
{#        <td>#}
{#            <form action="#" method="POST">#}
{#                <select onchange="this.form.submit();">#}
{#                <option selected>Parent...</option>#}
{#                <option value="1">Funny</option>#}
{#                <option value="1">--For kids</option>#}
{#                <option value="1">----For adults</option>#}
{#                <option value="2">Scary</option>#}
{#                <option value="3">Motivating</option>#}
{#                </select>#}
{#            </form>#}
{#        </td>#}
        <td><a href="{{ path('videos.delete', {'id': video.id}) }}"><i class="fas fa-trash"></i></a></td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
    {{ knp_pagination_render(videos) }}

{%  endblock %}
{% block custom_js %}
    <script>
        function delete_video(e,video_id)
        {
            e.preventDefault();
            if(confirm('Are you sure?'))
            {
                console.log(id);
                // delete video from vimeo
                // window.location.href = 'clear database record with Symfony';
            }
        }
    </script>
{% endblock %}