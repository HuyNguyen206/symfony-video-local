<nav class="my-2 my-md-0 mr-md-3">
    {% for category in categories %}
    <a class="p-2 text-dark" href="{{ path('categories.videos', {'id' : category.id, 'name': category.name|lower|slugify }) }}">{{ category.name }}</a>
    {% endfor %}
    <a class="p-2 text-dark" href="{{ path('admin') }}">My account</a>
</nav>